{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline window permissions; the app itself needs no native APIs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
